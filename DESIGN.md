# Design: 리베로 (Libero) — 9인제 동호회 캡틴 도구

> **캡틴은 클럽의 리베로입니다.** Every team has a libero. Yours is the captain.

Generated by /office-hours on 2026-04-08
Branch: main (greenfield, repo bootstrapped)
Repo: hykhyk189/Libero (private)
Status: APPROVED v4 (post-interview-re-anchor 2026-04-17, BUILD.md is the source of truth on tech)
Mode: Builder (CV / portfolio piece with month-3 strategic pivot to freemium SaaS)

> **⚠️ DOC PRECEDENCE NOTE (added v3):** This document is the **product** source of truth (problem statement, scope, user stories, success criteria, premises). For **tech decisions** (DI, caching, scraper hosting, auth flow, package structure, error patterns) the source of truth is **BUILD.md**. The two should agree; if they don't, BUILD.md is more recent and wins. Several tech decisions in this doc were superseded by the eng review and the CEO review — see "Decision Reversals" section below.

## Revision History

- **2026-04-08 (morning):** Initial draft, iOS-first, multi-manager Supabase, 4 modules, ~9-11 weeks.
- **2026-04-08 (afternoon):** **Android-first pivot.** Reason: Korean amateur 9인제 club captains (age 30-50) are overwhelmingly Android users, not iOS. Stack swapped to Kotlin + Jetpack Compose + Room + FCM + Sign in with Google. iOS is now v2, not MVP. Backend is unchanged. Considered Kotlin Multiplatform and Flutter; both rejected in favor of native Compose for the cleanest CV story and best Android UX. **Cost benefit:** Apple Developer Program $99/year → Google Play Console $25 one-time (lifetime).
- **2026-04-08 (afternoon, post-review):** **Spec review fixes applied.** Adversarial review caught three load-bearing issues: (1) Google Play 14-day closed-testing requirement for new personal accounts (adds ~2 weeks if applicable), (2) Apache POI cannot run in Deno Edge Functions (`.xls` fallback rewritten as on-device jExcelAPI; parsing stays on-device per privacy commitment), (3) domain made mandatory for App Links (was incorrectly listed as optional — see v3 reversal below). Plus consistency cleanups: dropped `apple_user_id` column, normalized polling cadence, added Hilt to tech stack (see v3 reversal), fixed Levenshtein library emphasis, named `jose` library for FCM JWT, added App Links + Sign in with Google setup gotchas, deferred Kakao Login to v1.1, moved fastexcel-reader smoke test into Week 1 Gate alongside Daum scraper feasibility.
- **2026-04-08 (evening):** **Project named "리베로" (Libero).** Reason: the libero is the volleyball position that plays defense in a different colored jersey, makes impossible saves look routine, and never gets the highlight reel — the perfect metaphor for what the captain does for the club. Ambition shifted from pure CV piece toward "real product with real revenue." **Revenue Model: Path A (Free MVP → freemium at month 6).** Pro tier (4,900원/mo) gates Module 4 auto-Band, multi-club, advanced filters, priority polling. Subscription scaffold tables added to the data model for week-2 plumbing. **Pre-commit homework:** verify "Libero" / "리베로" trademark on KIPRIS in Nice Class 9 + 42 before locking.
- **2026-04-08 (late evening):** **Eng review (`/plan-eng-review`) complete.** 21 decisions baked into BUILD.md (see BUILD.md "Eng Review Decision Log"). Most consequential reversals: Cloudflare Worker → Supabase Edge Functions, Hilt → Koin, Room → no-Room (in-memory StateFlow only), App Links + domain → 6-character invite codes (no domain), POI fallback → fastexcel-reader on-device only. DESIGN.md was NOT updated at this point — that gap was caught by the CEO review and fixed in v3.
- **2026-04-08 (post-late-evening) — v3:** **CEO review (`/plan-ceo-review`) complete.** 12 new decisions baked into BUILD.md (Decisions 22-33). Most consequential changes: (1) **reframed killer-feature thesis** — "operations layer for amateur club captains," NOT "shared source of truth between two equal managers" (multi-manager is now a *capability*, not the *headline*); (2) **Module 3 upgraded from W9 cut-valve to PROTECTED in MVP** (new cut valve = polish + Maestro coverage); (3) **PIPA captain-attestation pattern added** for third-party member PII; (4) **Sentry moved to W3** from W11; (5) **GitHub Actions CI added in W3**; (6) **acquisition prep block added in W12-13**; (7) **Haiku schema decision register baked in for W6** (multi-date posts, 수도권 전체, 오픈/친선, parser_confidence rubric); (8) **member deletion = anonymize pattern** (PIPA-compliant + audit trail). DESIGN.md is now formally aligned with BUILD.md (see "Decision Reversals" below).
- **2026-04-17 — v4:** **Interview re-anchor `/plan-ceo-review` complete.** Both own-club captain interviews done; 8 new decisions baked into BUILD.md (Decisions 34-41). Raw intake in `notes/captain-interview-combined.md`; synthesis in `notes/captain-interview-synthesis.md`. Most consequential changes: (1) **M2 center of gravity pivoted** from monthly-dues parser to reference-matched deposit classifier — new `payment_references` table lets captain curate `(label, amount, optional tournament_id)` pairs, import matches deterministically by amount, Haiku is tiebreaker-only, human confirms every row; (2) **members schema gains 6 nullable columns** (birth_date, gender, address, phone, jersey_size, jersey_number) to fulfill captain's "excel replacement" ask — PIPA covered by existing W4 captain-attestation pattern; (3) **주민번호 policy locked** — birth_date + gender as separate columns, never 7-char concatenation, never full 13-digit (PIPA 고유식별정보 landmine); (4) **attendance tracking placed** as v1.1 M3 extension (RSVP scope only — true attendance stays v2+); (5) **NotificationListenerService idea parked to TODOs** (Play Store restriction risk); (6) **multi-manager premise re-confirmed**; (7) **M1 demo framing validated** — Daum cafe app push is on; M1 leads on filter + structured data + one-tap Band draft, NOT raw speed. Drive-backed architecture was briefly proposed mid-review (to sidestep perceived 주민번호 PIPA landmine), then rejected once 주민번호 was recharacterized as first-7-only general personal info.

## Decision Reversals (v3 + v4 — these supersede earlier sections of this doc)

This section names every decision in the original DESIGN.md text that has been superseded by eng review, CEO review, or interview re-anchor. When you read the prose below and it contradicts something here, **this section wins** and the prose is historical.

| # | Original DESIGN.md said | Current truth (BUILD.md) | Why reversed |
|---|---|---|---|
| 1 | Cloudflare Worker for scraper, Plan B Fly.io + Playwright | Supabase Edge Functions (`scrape_daum_cafe`) | Eng review Decision 1: eliminates a whole second cloud platform; Supabase Cron + 150s timeout covers it |
| 2 | Room as local cache + offline `pending_writes` queue + Compose `collectAsState` from Room | NO Room in v1; in-memory `StateFlow` only; FCM data-payload trick mitigates the killer feature's hot path | Eng review Decision 2: cache invalidation is genuinely hard; v2 work (TODOS.md) |
| 3 | Hilt for Android DI | Koin, single `AppModule.kt`, no annotations | Eng review Decision 5: Hilt is overkill for ~15 ViewModels |
| 4 | Magic links via Android App Links + assetlinks.json + mandatory domain ($15/yr) | 6-character invite codes (alphanumeric, excluding 0/O/1/I), 7-day TTL, rotate button, `redeem_invite_code` Postgres function | Eng review Decision 3: Korean cultural fit, skips Android App Links setup hell, no domain needed |
| 5 | Buy a domain (`libero.kr`, `liberoapp.kr`, `libero.club`) | Do NOT buy a domain. Reverse-DNS package name `kr.libero.captain` is convention, not domain ownership | Eng review Decision 4: dissolved by Decision 3 |
| 6 | Privacy policy on Cloudflare Pages | Privacy policy on Notion public page or in-app `assets/privacy_policy.html` | Eng review Decision 4: no domain to host on |
| 7 | "Multi-manager from day one" as the killer-feature framing in "What Makes This Cool" | Reframed: "operations layer for amateur club captains" — multi-manager is a *capability*, not the *headline* | CEO review Decision 22: Module 1 and Module 3 work fine single-user; only Module 2 is genuinely multi-manager |
| 8 | Module 4 (Band auto-post) is the cut valve — drops if Modules 1+2 run over | Module 3 (renamed from Module 4 via eng review Decision 15) is **PROTECTED in MVP**, no longer the cut valve. New cut valve = polish + Maestro coverage | CEO review Decision 23: outside voice flagged that the reframed thesis is hollow at MVP if M3 cuts |
| 9 | Naver Band write-scope verification "by end of W9" | Verification moved to **W1 P0** alongside KIPRIS + captain interviews. With M3 protected, no fallback if approval is gated | CEO review Decision 24: 8 weeks of lead time vs 0, eliminates the W9 cut surprise |
| 10 | Cost itemization includes "Domain (mandatory for App Links) ~$15/year" | $0 recurring beyond Anthropic Haiku (~$1-3/mo) | Dissolved by reversals 4+5 |
| 11 | Effort table line "Supabase setup + auth + RLS + invite flow + App Links domain + assetlinks.json (1 wk)" | Supabase setup + auth + RLS + invite codes (no App Links, no domain) | Same |
| 12 | Effort table line "Android scaffold (Compose + Hilt + Room) + Sign in with Google + onboarding + Compose nav (1 wk)" | Android scaffold (Compose + Koin + StateFlow) + Sign in with Google + onboarding + Compose nav + Sentry + GitHub Actions CI | Eng review Decisions 2+5, CEO Decisions 27+28 |
| 13 | Reviewer Concerns lists 3 declined recommendations + Module 4 cut decision | CEO review added 4 more cross-model tensions resolved: M3 cut-valve contradiction (resolved by promoting to protected), killer-feature latency vs Daum push (resolved by adding interview question), M2 status-quo switching cost (resolved by adding interview question), PIPA third-party data exposure (resolved by W2/W4 attestation pattern) | CEO review outside voice |
| 14 | Captain interview script focuses on "show me your phone, walk me through last week, last missed tournament" | Same as before PLUS 3 new questions: Daum cafe push notification reality check, Module 2 status-quo switching cost test, PIPA notice baseline. See `notes/captain-interview-script.md` | CEO review Decision 33 |
| 15 | Member deletion not specified (implicit cascade) | Member deletion = anonymize: `display_name = '삭제된 회원'`, `kakao_handle = NULL`, `member_aliases` deleted, `member_payments` rows preserved for audit trail. PIPA-compliant + audit-intact | CEO Decision 30 / Step 0E temporal #2 |
| 16 | Members schema: `id, club_id, display_name, position, skill_level, kakao_handle, joined_date, notice_acknowledged_at` only | Members schema gains 6 nullable columns: `birth_date DATE`, `gender CHAR(1)`, `address TEXT`, `phone TEXT`, `jersey_size TEXT`, `jersey_number INT`. No column named for 주민번호 | Interview Re-anchor Decision 34 — captain's current Excel tracks these fields, "excel replacement" UX requires them |
| 17 | Implicit assumption that full 주민등록번호 would be stored if users asked | Explicit policy: birth_date + gender columns only, NEVER 7-char concatenation, NEVER full 13-digit. Full 주민등록번호 is PIPA 고유식별정보 and is never collected | Interview Re-anchor Decision 35 — captain's Excel "주민번호" is first-7 only (생년월일+성별, general personal info under PIPA); full 13-digit never enters the system |
| 18 | M2 framed as "모임통장 결제 추적기 — payment reconciliation" oriented around monthly `payment_cycles` | M2 framed as "reference-matched deposit classifier": captain curates `payment_references (label, amount, optional tournament_id)`, import matches deterministically by amount, Haiku is tiebreaker-only, human confirms every row. Monthly dues is one reference type among many (tournament fees, dinners, one-off collections) | Interview Re-anchor Decisions 36+37 — captain interview revealed annual dues dominate (20-30 of 40 members pay yearly), per-event payments are the real pain |
| 19 | "One-tap KakaoTalk 미납 안내" copied to clipboard as M2 closing demo | Demo moment shifts to "tournament payment → participants-only announcement → auto-match per event." Clipboard fallback still exists, demo video leads with the event-based workflow | Interview Re-anchor Decision 36 — event-based demo is more visceral |
| 20 | Attendance tracking not mentioned | Attendance = v1.1 M3 extension, RSVP scope only (read Band poll comments, map yes-RSVPs to members, produce attendance-rate view). True attendance (captain-marks-present at the gym) stays v2+ | Interview Re-anchor Decision 39 — captain asked; framing as M3 extension preserves 3-module MVP narrative |
| 21 | (Implicit) Excel is the only deposit input source considered | Excel is v1 input only. NotificationListenerService (read KakaoBank push notifications directly) is explicitly parked to TODOs — Play Store restricts the permission to "core use case" apps, general finance apps have had approvals revoked. Reference-matching algorithm is future-compatible if Play Store landscape shifts | Interview Re-anchor Decision 38 — captain suggested the mechanism; Play Store friction makes v1 risky |

## Problem Statement

Korean amateur 9인제 (9-man) volleyball clubs are run by 1-3 unpaid managers — typically a captain (캡틴) and a secretary (총무). Their job is split across four disconnected systems:

- **Daum cafes** (e.g., 'Let's go 생활체육배구') for tournament announcements
- **Naver Band** for club announcements, photos, attendance polls
- **KakaoTalk** for day-to-day chat
- **KakaoBank 모임통장** for collecting and reconciling 회비 (dues)

The captain manually checks the Daum cafe for new tournaments, drafts pre-formatted Band posts for attendance, and the secretary manually reconciles bank statements against the member roster every cycle. None of this is automated, and no existing tool unifies it. The user is a regular player in such a club and is building this as a CV/portfolio piece for a job hunt — the goal is a shipped native Android app with real captain users that demonstrates product judgment, technical depth, and follow-through. iOS port comes later as v2.

## What Makes This Cool (v3 — reframed by CEO review 2026-04-08)

**The reframed thesis:** Libero is **the operations layer for amateur club captains**. Korean 9인제 captains run their clubs across 4 disconnected systems (Daum cafe, Naver Band, KakaoTalk, KakaoBank) and the captain is doing invisible labor that nothing captures. Libero is the layer that doesn't replace any of those systems but connects them and saves the captain time on the work nobody sees. Multi-manager (captain + secretary) is a *capability*, not the *headline*.

**Module 1 leads on demo material.** A push notification arrives within hours of a tournament being posted in the cafe — the captain taps and lands instantly on the tournament detail screen with all data populated from the FCM payload (no spinner). The killer demo is the visceral "you got pushed → you're already in the detail screen." This is the screen you record for the CV demo video.

> **Reality check (per CEO review interview question A3):** if your captain has Daum cafe app push enabled, Libero is the *second*-fastest source not the first. The actual differentiator becomes the *filter and structured-data* play (regions, divisions, deadlines, dedup, parser confidence) — not raw speed. Verify in the captain interview before W1.

**Module 2 is the CV depth play.** The product judgment story: Vision OCR was the obvious answer — Libero routes around the entire fragile-OCR problem by using KakaoBank's `거래내역 다운로드` Excel export and fuzzy-matching names against the roster on-device. 100% data accuracy where Vision OCR would have been ~70-95% on a good day. **This is the design decision senior engineers respect**, the one you tell in interviews when asked "what was your hardest design call." The flow is the *real* multi-manager moment: secretary uploads from her phone (where the bank login lives), captain sees the result update live via filtered Realtime.

> **Reality check (per CEO review interview question B2):** does the secretary actually want to switch from her current Excel reconciliation, or is the status quo "5 minutes eyeballing the spreadsheet" already fine? Verify in the captain interview before W1.

**Module 3 is the breadth play and the Pro tier hook.** Auto-posts the weekly practice poll to Naver Band on a schedule. Captain configures once, the app runs the recurring post forever. The only feature that saves time *every single week* — load-bearing for the future Pro tier value prop. CEO review upgraded M3 from W9 cut-valve to PROTECTED in MVP because cutting it would leave the reframed thesis hollow at launch.

**Multi-manager is a capability that surfaces in Module 2.** Captain and secretary both sign in on their own phones, see the same data, edit the same records via filtered Realtime. Conflict handling is silent last-write-wins. v1 has zero role-based UI branching — every user inside a club sees identical screens with identical permissions, regardless of label. Role differentiation is v2 work (TODOS.md).

**Members never install.** Zero adoption friction, zero dual-tool fatigue. The app is invisible to the people the club serves. The captain processes member data on their behalf — and per PIPA, the captain is the data controller and must notify members. CEO review added the W4 captain-attestation checkbox to make this notice mechanism real, not bureaucratic.

**Built for the platform Korean amateur captains actually use.** Android-first, not iOS-first. Your captain owns a Galaxy S-something, not an iPhone. Match the user, not the developer aesthetic.

## Constraints

- Single developer, **~13 weeks committed** for MVP (see Effort section). Includes the Google Play 14-day closed-testing requirement (locked as applicable per user's homework — new personal Play Console account).
- Budget: **$25 one-time** (Google Play Console) + ~$1-3/mo (Anthropic Haiku) + $0 recurring otherwise. **No domain purchased** (Decision Reversal 4+5).
- Korean Google Play Store first, US Play Store later, **iOS App Store as v2 (post-MVP)**
- Native Android only (not web, not cross-platform — match the platform Korean amateur captains actually use; iOS port deferred to v2)
- Pre-build: 2 captain interviews mandatory before any code (own captain + own secretary, with the new CEO-review-added questions in `notes/captain-interview-script.md`)
- Personal Daum account used for scraping (user-accepted risk — see Reviewer Concerns)
- **Scraper polling: 3 hours, locked.** This is the chosen point inside the 2-4 hour stealth envelope (see Reviewer Concerns). Tune after first month of real use based on `time_from_post_to_notification` metric.
- **Min Android version: API 26 (Android 8.0)** — covers ~98% of active Korean Android devices in 2026, allows modern Compose features without legacy workarounds. Re-evaluate if your captain runs anything older.
- **Google Play Console: confirmed NEW personal developer account.** Google's Nov 2023 policy requires new personal accounts to run a closed test with **12+ testers for 14 continuous days** before production publishing is unlocked. This adds 2 weeks to the launch phase (W11-W13). The 14-day clock is calendar days, not work days.
- **Invite mechanism: 6-character alphanumeric codes** (Decision Reversal 4). NOT App Links, NOT magic links, NOT domain-based. Codes have 7-day TTL and can be rotated. KakaoTalk Sharesheet handles the share UX without any KakaoTalk SDK.

## Premises

1. **Korean 9인제 captains have a real, unmet workflow problem** — verified by user being in a 9인제 club with personal access to the captain
2. **Multi-manager (captain + secretary) is the right scope** — verified, single-user breaks how clubs operate
3. **The 'Let's go 생활체육배구' Daum cafe is technically scrapeable** — verified at the access level (등업 required, user has account, no posting requirement). **Not yet verified at the technical level — see Week-1 Prototype Gate.**
4. **Naver Band API is alive in 2026 and self-serve** — verified by user. **Posting scope still needs verification** (write endpoints historically require separate approval).
5. **KakaoBank 거래내역 download exists and exports a structured Excel file** — confirmed by user. **Exact column structure unverified** — gating Module 2 estimate.
6. **Tournament posts require LLM extraction** — agreed, free-form Korean prose can't be regexed reliably
7. **CV story is general portfolio (not Korean-specific or US-specific)** — bilingual is post-MVP work
8. **Korean amateur 9인제 captains (age 30-50) overwhelmingly use Android, not iOS.** Verified by Korean smartphone market data: ~70%+ Android share overall, even higher in the 30-50 cohort. iOS in Korea is concentrated in teens/20s as a fashion item. Building iOS-first means shipping to a minority of the target user base. Added 2026-04-08 PM as the basis for the Android-first pivot.

## Approaches Considered

### Approach A: Captain-only local-first
Single-user app, local-only, no auth, no backend except for the scraper.
- **Pros:** simplest to build, no auth complexity, no cloud cost beyond scraper
- **Cons:** secretary can't use it, breaks how real clubs operate
- **Effort:** ~6-7 weeks
- **Verdict:** Rejected — user confirmed multi-manager is the real need.

### Approach B: Multi-manager Supabase + 3-module MVP, native Android **← RECOMMENDED**
Captain + secretary both sign in via Sign in with Google (Kakao Login as alternate), shared club data via Supabase, native Kotlin + Jetpack Compose Android app.
- **Pros:** matches real workflow AND matches the platform Korean amateur captains actually use; supports a strong CV story (multi-tenant case study + product judgment + Korean cultural depth + native Android depth)
- **Cons:** Supabase + RLS + invite flow + FCM adds real auth/infra surface; iOS users (a minority of the target demographic) wait for v2
- **Effort:** ~9-10.5 weeks MVP (honest), +1-2 weeks for v1.1 lineup planner, +6-8 weeks for v2 iOS port (separate)
- **Reuses:** Supabase auth, Sign in with Google, fastexcel-reader for Excel parsing, Apache Commons Text for fuzzy match, Claude Haiku for tournament parsing, Naver Band Open API, Supabase Edge Functions for FCM dispatch

### Approach C: Kotlin Multiplatform (KMP) — share business logic across Android + iOS (lateral, considered 2026-04-08 PM)
Write the data layer, networking, models, and module logic once in Kotlin. Compose UI for Android now, SwiftUI UI for iOS later, sharing 60-70% of the code via KMP.
- **Pros:** When you do ship iOS as v2, you reuse most of the work. Stronger long-term CV story ("shipped Android first, ported to iOS reusing 70% of business logic via KMP"). Great if you have a clear iOS roadmap.
- **Cons:** KMP setup adds 1-2 days of friction now. Swift interop has rough edges, especially around Coroutines + callbacks. Splits your attention across two language ecosystems.
- **Verdict:** Rejected for MVP. Right tool for v2 if iOS becomes a confirmed roadmap item, not a "maybe later."

### Approach D: Flutter — single Dart codebase, both stores (lateral, considered 2026-04-08 PM)
- **Pros:** Ships to Android and iOS simultaneously, fastest path to "any platform."
- **Cons:** Not native. Korean tech employers (Naver, Kakao, Toss, Coupang) mostly run native shops. The CV story gets blurry — "I built a Flutter app" reads less senior than "I built a native Android app." Also gives up the platform-depth angle entirely.
- **Verdict:** Rejected — the CV story rests on platform depth, not platform breadth.

## Recommended Approach: B — Multi-manager Supabase, native Android, MVP

### Week-1 Prototype Gate (CRITICAL)

> **⚠️ v3 update (eng review 2026-04-08):** the original DESIGN.md described 2 W1 gates. Per Eng Review Decision 13 there are now **5 gates** (1A-1E). BUILD.md W1 has the full prescription with code samples. The summaries below are the headline; trust BUILD.md for execution detail.

Before any other build work, prove all 5 load-bearing assumptions in W1. If any fails AND no fallback works, the architecture is wrong.

**Gate 1A — Daum scraper feasibility.** Verify that a `fetch` call (from a Supabase Edge Function — NOT Cloudflare Workers; see Decision Reversal 1) against the cafe with logged-in Daum cookies returns post HTML. Document cookie freshness window. If fetch-only fails, fallback is the same edge function with a server-side fetch retry pattern.

**Gate 1B — fastexcel-reader smoke test on a real KakaoBank export.** Module 2's architecture rests on parsing `.xlsx` correctly on-device. Get a real export from your secretary, run fastexcel-reader in a JVM scratchpad. If it chokes on Korean column headers, merged cells, or summary rows, fallback is POI XSSF on-device (adds ~5-10MB to APK).

**Gate 1C — FCM delivery on Samsung with battery optimization.** The #1 reason Korean Android push apps fail. Test on a real Galaxy with default battery settings; expect ≥4/5 delivery success. If fail, mandatory in-app guide screen for 잠자지 않는 앱.

**Gate 1D — Credential Manager + Sign in with Google on Korean OEM.** Test the modern Credential Manager API on at least 2 Galaxy devices. If fail, fall back to the legacy `GoogleSignInClient` (deprecated but works everywhere).

**Gate 1E — Supabase Edge Function latency from Korea.** Verify p50 warm <100ms and p99 cold <1s. If cold-start latency is over budget, mitigate via a heartbeat cron to keep functions warm.

**Gate decision:** any gate fails AND fallback fails → STOP, rethink, do not proceed to W2.

### System Architecture

```
[Android App (Jetpack Compose)]
    ↕ Supabase Kotlin SDK (Auth + Postgres + Realtime)
    ↕ Firebase Cloud Messaging (FCM)

[Supabase] ← source of truth (single platform — no Cloudflare, no Fly.io; see Decision Reversal 1)
    - Postgres (clubs, members, payments, tournaments, prefs, aliases, payment_references)
    - Auth (Sign in with Google; Kakao Login deferred to v1.1)
    - Edge Functions:
        · scrape_daum_cafe — pg_cron every 3h, fetches Daum, calls Haiku, writes tournaments
        · dispatch_tournament_match — Postgres trigger fires on tournaments insert, parallelized FCM fan-out
        · post_band_poll — Supabase scheduled trigger, calls Naver Band Open API (if approved)
        · redeem_invite_code — 6-char code validation
    - RLS policies enforce per-club data isolation
    - pg_cron orchestrates scraper schedule

[Anthropic Claude Haiku] — called from scrape_daum_cafe to extract tournament fields
[Firebase Cloud Messaging] — push delivery only (no Firestore, no Firebase Auth — Supabase Auth handles login)
```

### Data Model (sketch)

```sql
clubs (
  id uuid PK,
  name text,
  region text,
  created_at timestamptz
)

users (
  id uuid PK,                    -- supabase auth user id (from auth.users.id, OAuth provider-agnostic)
  display_name text
)

club_managers (
  club_id uuid FK clubs.id,
  user_id uuid FK users.id,
  joined_at timestamptz,
  PRIMARY KEY (club_id, user_id)
)

club_invites (
  token uuid PK,
  club_id uuid FK clubs.id,
  created_by uuid FK users.id,
  expires_at timestamptz,        -- 7-day TTL
  used_at timestamptz NULL       -- single-use
)

members (
  id uuid PK,
  club_id uuid FK clubs.id,
  display_name text,
  position text,                 -- 라이트/센터/레프트/리베로
  skill_level text,
  kakao_handle text NULL,
  joined_date date,
  -- Interview Re-anchor Decision 34 (2026-04-17): 6 new nullable columns, replaces captain's Excel
  birth_date date NULL,          -- first 6 digits of 주민번호 worth of info; never store full 13
  gender char(1) NULL,           -- 'M' | 'F' | NULL (7th digit of 주민번호 worth; never store concatenated)
  address text NULL,
  phone text NULL,
  jersey_size text NULL,         -- "S" | "M" | "L" | "XL" | custom
  jersey_number int NULL,
  notice_acknowledged_at timestamptz NULL  -- W4 PIPA attestation, CEO Decision 25
)

member_aliases (
  member_id uuid FK members.id,
  alias_text text,               -- e.g., "철수형" matches "김철수"
  PRIMARY KEY (member_id, alias_text)
)

payment_cycles (
  id uuid PK,
  club_id uuid FK clubs.id,
  label text,                    -- "2026년 4월 회비 5만원"
  amount numeric,
  due_date date
)

member_payments (
  cycle_id uuid FK payment_cycles.id,
  member_id uuid FK members.id,
  paid boolean,
  paid_at timestamptz NULL,
  source text,                   -- "manual" | "excel_import" | "excel_confirmed"
  updated_by uuid FK users.id,
  updated_at timestamptz,
  PRIMARY KEY (cycle_id, member_id)
)

-- Interview Re-anchor Decision 36 (2026-04-17): reference-matched deposit classifier
payment_references (
  id uuid PK,
  club_id uuid FK clubs.id,
  label text NOT NULL,                    -- "월회비" | "대전 대회비" | "4월 회식비"
  amount int NOT NULL,                    -- KRW, exact match target
  tournament_id uuid FK tournaments.id NULL,  -- optional, set if this reference is tournament-linked
  cycle_id uuid FK payment_cycles.id NULL,    -- optional, auto-created when tournament_id is set
  active boolean NOT NULL DEFAULT true,   -- captain deactivates once fully collected
  created_at timestamptz DEFAULT now(),
  deactivated_at timestamptz NULL
)
-- Index: (club_id, active, amount) for fast lookup during import
-- RLS: club_managers can SELECT/INSERT/UPDATE/DELETE where club_id matches their club

tournaments (
  id uuid PK,
  cafe_post_id text UNIQUE,      -- stable dedup key (Daum 게시글 번호)
  title text,
  date date,
  location text,
  region text,                   -- enum: 서울/경기/인천/강원/충북/충남/...
  fee numeric,
  division text,                 -- 초급/중급/상급
  deadline date,
  raw_post_text text,            -- for re-parsing if needed
  parsed_at timestamptz,
  parser_confidence numeric      -- 0..1 from Haiku
)

tournament_preferences (
  id uuid PK,
  club_id uuid FK clubs.id,
  regions text[],                -- multi-select 시/도
  date_range_days int,           -- "next 60 days"
  divisions text[],
  avoid_weekdays int[],          -- 0=Sun..6=Sat
  enabled boolean
)

push_tokens (
  user_id uuid FK users.id,
  device_token text,             -- FCM registration token
  platform text,                 -- "android" at MVP, "ios" added in v2
  updated_at timestamptz,
  PRIMARY KEY (user_id, device_token)
)

band_polls (
  id uuid PK,
  club_id uuid FK clubs.id,
  band_group_id text,
  cron_expression text,          -- weekly schedule
  template_text text,
  last_posted_at timestamptz,
  enabled boolean
)

-- Subscription scaffold (added 2026-04-08 evening for Path A freemium plumbing)
-- Tables exist from week 2; gating logic stays dormant until month 6 flip.

subscription_tiers (
  id text PK,                    -- "free" | "pro"
  display_name text,             -- "무료" | "리베로 프로"
  monthly_price_krw int,         -- 0 | 4900
  yearly_price_krw int,          -- 0 | 49000
  features jsonb                 -- feature flags: {auto_band:true, multi_club:true, ...}
)

club_subscriptions (
  club_id uuid PK FK clubs.id,
  tier_id text FK subscription_tiers.id NOT NULL DEFAULT 'free',
  started_at timestamptz,
  current_period_end timestamptz NULL,    -- null = free forever
  payment_provider text NULL,             -- "tosspayments" | "kakaopay" | null
  external_subscription_id text NULL,     -- provider-specific ID
  cancelled_at timestamptz NULL
)

-- RLS: club_subscriptions readable by club_managers, writable only by service role
--      (subscription state is managed by webhooks from the payment provider, never by client)
```

**RLS policies (sketch):** every table with `club_id` enforces `club_id IN (SELECT club_id FROM club_managers WHERE user_id = auth.uid())`. Tables without `club_id` (`users`, `push_tokens`) enforce `user_id = auth.uid()`. `tournaments` is global (not club-scoped) but read-only to clients; only the scraper service-role key writes.

**Invite flow (end-to-end) — v3, post-Decision-Reversal-3 (6-char codes, NOT App Links):**
1. Existing manager taps "Invite secretary" in club settings → app calls `redeem_invite_code` Postgres function (server-side) to mint a fresh 6-char alphanumeric code (excluding 0/O/1/I), with 7-day TTL. The code is stored on the `clubs` row itself (`invite_code` + `invite_code_expires_at` columns).
2. App constructs a Korean share message: `"[리베로] 가입 코드: ABC123 (7일 유효). 다운로드: <Play Store URL>"` and launches an Android Sharesheet via `Intent.ACTION_SEND` with `type = "text/plain"`. KakaoTalk appears in the picker because it's installed on every Korean phone and registers an `ACTION_SEND text/plain` intent filter. **No KakaoTalk SDK needed. No domain needed. No App Links needed.**
3. New manager opens the app fresh, signs in with Google, picks "기존 클럽 가입" on the create-or-join branch, types the 6-char code into a text input, taps confirm.
4. App calls `redeem_invite_code(p_code)` Postgres function → function validates: code matches an existing `clubs.invite_code`, `invite_code_expires_at > now()`, current user not already in `club_managers` → inserts `(club_id, user_id, joined_at)` into `club_managers` → returns club info. Code is single-use; rotating it is one tap in club settings.
5. New manager lands in the main app, now sees the shared club's data.
6. **Expiry handling:** code expired → function raises Korean exception "이 가입 코드는 만료되었습니다. 캡틴에게 새 코드를 요청하세요."
7. **Code-rotation:** captain taps "코드 새로 만들기" in club settings → app updates `invite_code` to a new random 6-char + sets `invite_code_expires_at = now() + 7 days`. Old code immediately raises "expired."

**Why 6-char codes, not magic links + App Links + domain:** Korean cultural fit (codes match how 카카오/네이버 invite flows work), skips the entire Android App Links setup (assetlinks.json hosting, SHA-256 fingerprint dance, domain purchase). See Decision Reversal 4 for full rationale.

**Sign in with Google setup notes (gotchas):**
- Supabase's Google provider needs an **OAuth 2.0 Web client ID** configured in Google Cloud Console (used as the `client_id` Supabase passes to Google's OAuth endpoint). It also needs an **Android client ID** with the SHA-1 of the app's signing key, registered in the same Google Cloud project. **Both are required.** Most first-time errors here come from forgetting the Android client ID.
- Use the Play App Signing key SHA-1, NOT the upload key SHA-1, for the production Android client ID. Use the debug keystore SHA-1 for local development. Register both.

**Realtime sync (multi-manager edits):** Supabase Realtime subscriptions on `member_payments`, `payment_cycles`, `payment_references`, `members`, and `tournament_preferences`, **server-side filtered by `club_id`** (Eng Review Decision 21 — filtered subscriptions, not whole-table). When manager A edits a row, manager B's app receives the update within 1-2 seconds via WebSocket and updates the in-memory `StateFlow` directly (no Room layer). **Conflict handling is silent last-write-wins** at row granularity. The loser does not see a toast or warning, just the new value via the realtime subscription. Per-field change tracking ("updated by [name]") was considered and rejected as not worth the complexity at MVP scale.

**Offline writes (v3 — superseded by Decision Reversal 2):** the original v1 design used Room as the local cache + a `pending_writes` queue. Eng review reversed both. v1 ships with **in-memory `StateFlow` only**, sourced directly from the Supabase Kotlin SDK. Cold-start UI shows a brief spinner; the killer-feature push-to-detail path is mitigated via the FCM data-payload trick (full tournament ships in the push, no fetch needed). Persistent cache (DataStore + tiny Room) is v2 work — see TODOS.md "Persistent cache layer." Offline writes are not supported in v1; if the network is down, writes fail with a Korean error toast and the user retries.

### Push Dispatch Path

- **Scraper writes** new tournament rows to Supabase via service-role key
- **Postgres trigger** on `tournaments` insert calls a **Supabase Edge Function** `dispatch_tournament_match`
- Edge Function: query all `tournament_preferences` where regions/division/date match → for each matched club → query `club_managers.user_id` → query `push_tokens` → call **FCM HTTP v1 API** (`https://fcm.googleapis.com/v1/projects/{project}/messages:send`) with the device token and notification payload
- **FCM auth:** Edge Function uses a Firebase Admin SDK service account JSON (download from Firebase Console → Project Settings → Service Accounts; store the entire JSON as a Supabase secret named `FIREBASE_SERVICE_ACCOUNT_JSON`). Mint a short-lived OAuth bearer token by signing a JWT with the service account private key (RSA-SHA256), then exchange it at `https://oauth2.googleapis.com/token`. Use the [`jose`](https://deno.land/x/jose) Deno library for JWT signing (RSA-SHA256 in 5 lines, vs ~40 lines of Web Crypto glue). Cache the resulting access token for its 1-hour TTL inside the Edge Function via a module-level variable. Required service account role: **Firebase Admin SDK Administrator Service Agent** (auto-assigned when downloading the service account JSON from Firebase Console — do not assign manually).
- Single dispatch path. All scraping, push dispatch, and Band API calls live in Supabase Edge Functions — no second cloud platform (Decision Reversal 1).

### Modules (numbering — v3)

**Current numbering (post-eng-review Decision 15):** the MVP has 3 modules — **Module 1 (Tournament Notifier)**, **Module 2 (Reference-Matched Deposit Classifier)**, **Module 3 (Naver Band Auto-Post)**. The original v1 doc had a 1/2/4 layout with "Module 3" reserved for the Lineup Planner; eng review renumbered the MVP to 1/2/3 and the Lineup Planner is now v1.1 fast-follow work referenced by name, not number.

When prose below uses "Module 4" (and there are leftover stragglers in v3 cleanup), it means **the same thing as Module 3 today** — Naver Band auto-post. The "v3 numbering note" further down explains the history; ignore that note if you're starting fresh, the canonical labels are Module 1 / Module 2 / Module 3 / Lineup Planner-as-v1.1.

#### Module 1 (KILLER): 대회 알림 — Tournament Notifier
- Captain or secretary sets `tournament_preferences` per club (regions, date range, divisions, avoid weekdays)
- Backend cron pulls Daum cafe → LLM-parses → upserts to `tournaments` (dedup by `cafe_post_id`) → Edge Function dispatches FCM to all matching clubs' managers
- Tap notification → tournament details view in-app → save to watchlist
- One-tap "draft Naver Band 대회 출전 투표" → pre-formatted text + (optionally auto-post via Module 3 infrastructure if user enabled it)
- Captain marks "applied" or "skipped"

**Acceptance criteria:**
- Given a new tournament post in the cafe, when the cron runs and the post matches a club's preferences, then a push notification is delivered to all of that club's managers within 3 hours + 60s of the post being published.
- Given the captain taps the notification, when the app opens, then the tournament details view shows date/location/fee/division/deadline.
- Given a duplicate post (same `cafe_post_id`), when the cron runs, then no duplicate row is created and no duplicate push is sent.

**Haiku extraction JSON schema:**
```json
{
  "title": "string",
  "date": "YYYY-MM-DD or null",
  "location": "string or null",
  "region": "one of: 서울/경기/인천/강원/충북/충남/대전/세종/전북/전남/광주/경북/경남/대구/울산/부산/제주",
  "fee": "integer KRW or null",
  "division": "초급|중급|상급|무관|null",
  "deadline": "YYYY-MM-DD or null",
  "confidence": "float 0..1 (Haiku self-rates extraction confidence)"
}
```
**Low-confidence handling:** rows with `confidence < 0.6` are stored but NOT dispatched as push notifications. They appear in an "uncertain matches" tab in the app for the captain to review manually. This avoids spamming captains with garbage parses.

**Region enum (locked):** the 17 시/도 — 서울, 경기, 인천, 강원, 충북, 충남, 대전, 세종, 전북, 전남, 광주, 경북, 경남, 대구, 울산, 부산, 제주. No 시/군/구 sub-granularity at MVP. The captain selects one or more 시/도 in their preferences; the LLM is told to pick the closest match from this enum.

#### Module 2: 모임통장 결제 추적기 — Reference-Matched Deposit Classifier (v4)

> **⚠️ v4 note (2026-04-17):** the prose below describes the original monthly-dues framing. Per **Decision Reversal 18**, M2's center of gravity has shifted to a **reference-matched deposit classifier**: captain curates `payment_references` pairs like "월회비 = 50,000원" or "대전 대회비 = 20,000원"; imports match deterministically by amount; Haiku is tiebreaker-only; human confirms every row. Monthly dues is one reference type among many. Implementation details live in BUILD.md W8 + W9 (Interview Re-anchor Decisions 36-37). Monthly-dues workflow below still works — it's just one of many cycle types now, not the primary framing.
- Captain or secretary creates payment cycles ("2026년 4월 회비 5만원")
- Member list with paid/unpaid toggle (manual)
- **Excel upload flow:**
  1. Secretary requests 거래내역 다운로드 from KakaoBank → email arrives with `.xlsx` (verified in 2026 — KakaoBank exports xlsx, not legacy xls)
  2. Secretary uploads file via Android Storage Access Framework (`ACTION_OPEN_DOCUMENT` with MIME filter for `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`)
  3. App parses with **fastexcel-reader on-device** (lightweight streaming `.xlsx` parser, ~1MB, Kotlin/JVM). Parsing is on-device because the raw bank export must never leave the user's phone (PIPA + privacy commitment). Only the parsed match results sync to Supabase.
  4. **`.xls` fallback (only if Week 1 verification finds KakaoBank still emits legacy format):** use **jExcelAPI** (~500KB, Apache 2.0) on-device. Apache POI HSSF works on Android but is ~10MB and not worth the APK weight. **Apache POI in a Supabase Edge Function is NOT a fallback option** because Edge Functions run on Deno, not JVM. Server-side parsing also breaks the privacy commitment (raw bank data would transit the server). Hard rule: parsing stays on-device.
  5. Fuzzy-match each row's name against `members.display_name + member_aliases.alias_text`
  6. **Match rule:** **normalized Levenshtein similarity** (1 - distance/max_length, on `[0, 1]`) ≥ **0.85** = auto-match. Tie (multiple members at ≥0.85) = prompt. Below 0.85 = prompt. **Library:** hand-rolled normalized Levenshtein in ~30 lines of Kotlin (preferred — no APK bloat). Apache Commons Text `LevenshteinDistance` is a fine fallback if you'd rather pull a dependency, but it adds ~1.5MB to the APK for one function. Code sketch:
     ```kotlin
     fun similarity(a: String, b: String): Double {
       val distance = levenshtein(a, b)  // standard 30-line DP
       val maxLen = maxOf(a.length, b.length)
       return if (maxLen == 0) 1.0 else 1.0 - distance.toDouble() / maxLen
     }
     ```
  6. Auto-marks confident matches as `paid = true, source = 'excel_import'`
  7. Shows "X auto-matched, Y need confirmation" sheet → user taps each ambiguous row to assign → row saved with `source = 'excel_confirmed'`
  8. When user assigns an unmatched name to a member, **insert into `member_aliases`** so future imports auto-match
  9. Parsed results sync to Supabase via standard API (the parsing is local; the storage is shared so the secretary's confirmation is visible to the captain)
- One-tap "draft KakaoTalk 미납 안내" → copies polite reminder text to **clipboard** (no KakaoTalk deep-link API exists; clipboard is the standard pattern)

**Acceptance criteria:**
- **Test dataset:** 20 transactions from a real KakaoBank export against a 25-member roster, where ≥70% of transactions are paid by members whose roster name matches their bank-display name within ≤2 character edits (the realistic case in a Korean amateur club). On this dataset, ≥80% of rows must auto-match. The remaining ~20% are presented for manual assignment.
- Given the secretary assigns an unmatched row to a member, when she uploads the next month's file, then that name auto-matches without prompting (alias persisted to `member_aliases`).
- Given two managers edit the same `member_payments` row simultaneously, when both save, then last-write-wins applies silently and the loser sees the winner's value via Supabase Realtime within 2 seconds.

#### Module 3: 주간 연습 투표 헬퍼 — Practice Poll (was Module 4 in v1/v2; PROTECTED in MVP per CEO Decision 23)
- Captain configures recurring weekly poll: court name, time, day-of-week, message template
- Stored in `band_polls` with cron expression
- Supabase Edge Function `post_band_poll` runs on Supabase scheduled trigger, calls Naver Band Open API write endpoint
- Reactions/comments visible in-app via Band API read endpoints (v1.1 attendance extension)
- **Fallback if API call fails:** Edge Function writes failure to a `band_post_failures` queue → FCM notification to captain "Today's poll didn't auto-post, tap to copy and paste manually"
- **Fallback if Naver Band API access is permanently denied** (real possibility post-2-rejections in pre-W1): ship M3 with manual clipboard-paste implementation per BUILD.md Part 0.7 buckets (a)/(b), revisit API integration in v1.1 if 사업자등록 path opens. M3 is PROTECTED — it ships in some form, even if degraded.

**Pre-build verification (now in active escalation, not just homework):** Naver Band Open API write scope verification was moved to **W1 P0** per CEO Decision 24 — 8+ weeks of lead time before W10. As of late April 2026, Band has rejected two app-creation submissions without disclosed reasons; escalation via developers.band.us contact form is in flight (see PROGRESS.md). Module 3's degraded fallback paths above are now load-bearing, not theoretical.

**Acceptance criteria:**
- Given an enabled `band_polls` row with cron `0 9 * * 1` (Monday 9am KST), when Monday 9am arrives, then a poll is posted to the configured Band group within 5 minutes.
- Given the API call fails, when the failure occurs, then the captain receives a push notification with a one-tap "copy poll text" action.

#### Lineup Planner — DEFERRED to v1.1 (was originally numbered "Module 3" in v1; renumbered out)
Drag-and-drop 9인제 lineup, save templates, share as image to KakaoTalk. Ships ~2 weeks after MVP launch. Referenced by name in BUILD.md and TODOS.md — no module number in v3.

> **v3 numbering history (kept for reference only):** the v1/v2 doc had Module 1 (Tournament), Module 2 (Payments), Module 3 (Lineup Planner — deferred), Module 4 (Band auto-post — was cut-valve). Eng review Decision 15 renumbered the MVP to 1/2/3 (Tournament / Payments / **Band auto-post**), and the Lineup Planner became a v1.1 item without a module number. CEO Decision 23 then upgraded the new Module 3 (Band auto-post) from cut-valve to PROTECTED. BUILD.md is canonical. Stop using "Module 4" — it's been Module 3 since 2026-04-08.

### Tech Stack (v3 — superseded entries marked, see Decision Reversals above)

| Layer | Choice | Notes |
|---|---|---|
| Android UI | Jetpack Compose | Native, min SDK 26 (Android 8.0) |
| Android data | **Supabase Kotlin SDK + in-memory `StateFlow` only — NO Room in v1** | Eng review Decision 2. Source of truth = Supabase. v2 adds DataStore + tiny Room (TODOS.md). FCM data-payload trick mitigates the killer feature's hot path. |
| Android DI | **Koin, single `AppModule.kt`** | Eng review Decision 5 (NOT Hilt). Plain Kotlin, no annotations, faster Gradle builds. |
| Android error pattern | `Resource<T>` sealed class with Loading/Success/Error | Eng review Decision 6. Compose `when` exhaustiveness enforces all 3 states. |
| Android crash reporting | **Sentry SDK in W3** (CEO Decision 27) | Was W11 polish, moved up because W11 is the busiest week. Free tier 5k events/month. |
| CI/CD | **GitHub Actions in W3** (CEO Decision 28) | `.github/workflows/android.yml` runs lint + unit tests + Maestro on every PR. Was deferred in BUILD.md Part 5; CEO review added it. |
| Auth | Sign in with Google → Supabase Auth | Kakao Login deferred to v1.1 (eng review). Use Credential Manager API on Android 14+ with legacy GoogleSignInClient as fallback if W1 Gate 1D fails. |
| Backend DB | Supabase Postgres (free tier) | Free tier covers MVP scale |
| Backend functions | Supabase Edge Functions (Deno) | Push dispatch, Band API, invite redemption, **scraper** (NOT Cloudflare Workers — Decision Reversal 1) |
| Scraper | **Supabase Edge Function `scrape_daum_cafe` + pg_cron 3h** | Eng review Decision 1. NOT Cloudflare Workers, NOT Fly.io. Plan B if fetch-only fails: same edge function with a server-side fetch retry pattern. |
| Tournament parsing | Anthropic Claude Haiku via API | JSON-mode, schema-validated via Zod (Section 2 fix from CEO review). Schema decision register: see BUILD.md CEO Decision 29. |
| Excel parsing | fastexcel-reader (on-device, Kotlin/JVM) | Verified in Week 1 Gate 1B; `.xls` fallback is jExcelAPI on-device. Parsing stays local per PIPA commitment. |
| Push | FCM HTTP v1 API via Supabase Edge Function | Service-account JWT signed with `jose`, 1h cached bearer. Parallelized via `Promise.allSettled` (eng review Decision 21). Data-payload trick (eng review Decision 20) ships full tournament in the push payload. |
| Band integration | Naver Band Open API | **W1 P0 verification (CEO Decision 24)** of write scope, NOT W9. Module 3 PROTECTED in MVP (CEO Decision 23). |
| Image share to KakaoTalk | Android `Intent.ACTION_SEND` with `com.kakao.talk` package | Standard intent flow, no SDK needed |
| Invite mechanism | **6-character alphanumeric codes via `redeem_invite_code` Postgres function** | Eng review Decision 3. NO App Links, NO magic links, NO domain. KakaoTalk Sharesheet for distribution. |
| PIPA member attestation | **`members.notice_acknowledged_at` column + W4 captain checkbox** (CEO Decision 25) | Captain attests she has informed each member that their info is stored. Required by PIPA legitimate-interest basis. |
| Member deletion | **Anonymize, not cascade or soft-delete** (CEO Decision 30) | `display_name = '삭제된 회원'`, `kakao_handle = NULL`, `member_aliases` deleted, payments preserved for audit trail. |
| Localization | Korean only at MVP | English added at US launch (post-MVP); use Compose `stringResource` from day 1; `HardcodedText` lint promoted to error (eng review Decision 8) |
| Build | Android Studio + Gradle (Kotlin DSL) | Standard Android stack |

### Effort (honest revision)

The reviewer flagged the original 7-8 week estimate as optimistic. Realistic breakdown:

> **⚠️ v3 update:** the Effort table below is the original v1/v2 estimate. Several rows are superseded by eng review (Hilt → Koin, Room → in-memory, App Links → invite codes, see Decision Reversals 2-5). BUILD.md is the canonical week-by-week and totals ~13 weeks including 14-day closed-test calendar window. Treat this table as historical context, not the live plan.

| Phase | Weeks |
|---|---|
| Captain interview (homework, before code) | included in pre-build, not coding |
| **Week 1 GATE:** 5 gates per Eng Review Decision 13 (Daum scraper, fastexcel, FCM-on-Samsung, Credential Manager OEM, Supabase latency) | 1 |
| Supabase setup + auth + RLS + invite codes (6-char, no App Links, no domain) | 1 |
| Android scaffold (Compose + **Koin** + **in-memory StateFlow**) + Sign in with Google + onboarding + Compose nav + Sentry + GitHub Actions CI | 1 |
| Module 1 (Tournament Notifier): preferences UI + scraper integration + LLM parser + dispatch + FCM setup + notification handling | 3-4 |
| Module 2 (Reference-Matched Deposit Classifier): cycle UI + roster + reference table CRUD + Excel upload (SAF) + reference-match + fuzzy name-match + alias dict + manual confirmation UX | 2-2.5 |
| Module 3 (Band auto-post): scheduling UI + Edge Function + Band API integration + failure path | 1-1.5 |
| Korean Google Play data safety form + privacy policy hosting + review iteration | 0.5-1 |
| **Subtotal (build + submit)** | **~10-11 weeks** |
| **Google Play 14-day closed-testing window** (LOCKED — new personal account confirmed) | **+2 weeks (overlap-able with W11 polish)** |
| **Total MVP** | **~13 weeks** (BUILD.md is canonical) |
| v1.1 Lineup Planner fast follow | +1-2 |
| v1.1 Kakao Login provider added alongside Google | +0.5 |
| v1.1 Attendance tracking (M3 extension, RSVP scope) | +1 |
| v2 (post-MVP): iOS port via SwiftUI rewrite | +6-8 (separate, not counted in MVP) |

**Schedule slack policy (v3 — superseded by CEO Decision 23):** the original v1/v2 designated **Module 4** (now Module 3, Band auto-post) as the cut valve. CEO Decision 23 reversed this — Module 3 is now **PROTECTED in MVP**. The new cut valve is **polish + Maestro test coverage**: if W4-W9 modules run over, you ship with thinner test coverage and rougher polish, NOT with a missing module. The reframed thesis ("operations layer for amateur club captains") is hollow at MVP if any of M1/M2/M3 is missing. The 14-day Play closed-testing window is calendar days, so the closed test must start at least 14 days before intended production launch.

### Cost itemization (v3)

| Item | Cost |
|---|---|
| Google Play Console | **$25 one-time** (lifetime, no annual renewal) |
| Supabase | $0 (free tier covers MVP scale: 500MB DB, 5GB egress, 2M Edge Function invocations, 50k MAU) |
| ~~Cloudflare Workers~~ | ~~$0~~ — **NOT USED, dissolved by Decision Reversal 1** |
| Anthropic Claude Haiku | ~$1-3/mo at expected scale (~50-200 posts/day × ~$0.001/parse) |
| Sentry | $0 (free tier 5k events/month, added in W3 per CEO Decision 27) |
| Firebase Cloud Messaging | $0 (Spark plan, unlimited notifications) |
| Firebase project (Auth+FCM only, no Firestore) | $0 (Spark plan) |
| ~~Domain~~ | **NOT PURCHASED, dissolved by Decision Reversals 4+5** |
| **Total recurring** | **$1-3/mo** (Anthropic Haiku only) |
| **Total upfront** | **$25** (Play Console one-time) |

## Privacy / PIPA Considerations (Korea)

A Korean app handling personal data (member names, KakaoTalk handles, payment records, bank transaction data) is subject to PIPA (개인정보 보호법). Required:

- **Privacy policy** in Korean, linked from app + Play Store listing. Specifies what data is collected, why, where it's stored, and retention.
- **Minimum data collection**: member rosters and payment data are stored in Supabase (encrypted at rest). Bank export Excel files are parsed locally; only the parsed match results (name, amount, paid status) are synced — never the raw Excel.
- **Daum cookies** are stored as Supabase Edge Function secrets via `supabase secrets set` (encrypted at rest). They're scraping infrastructure, not user data, but should be rotated per the cookie freshness window.
- **No third-party analytics** at MVP. Add later only with explicit consent.
- **Data deletion**: managers can delete the club entirely from settings → cascade deletes all related rows.
- **Google Play Data Safety form**: declare collected data types — name (linked to user), contacts (member roster), financial info (payment records). Declare encryption in transit (Supabase HTTPS) and at rest (Postgres). Declare data deletion path. Korean Play Store reviewers scrutinize financial-data apps; the privacy policy URL must resolve to a real Korean-language page on submission day.
- **Android-specific permissions:** `INTERNET` (always), `POST_NOTIFICATIONS` (Android 13+, request at runtime on first launch). **Do NOT request `READ_MEDIA_IMAGES` at MVP** — the lineup template thumbnails come with Module 3, which is deferred to v1.1. Use SAF (`ACTION_OPEN_DOCUMENT`) for the KakaoBank Excel upload, which does not require any storage permission. Smaller permission set = less Play Store data safety friction = faster review.

## Open Questions

These do not block the design but need resolution before or during build. Resolved entries are marked ✅ inline so the active list stays scannable.

1. **Daum cookie freshness window** — answered by Week-1 Gate 1A.
2. **KakaoBank Excel file type and column structure** — get a real export from secretary before building parser. (Now part of W1 Gate 1B.)
3. ✅ **Naver Band Open API write scope** — verification moved to W1 P0 (CEO Decision 24); 2 submissions rejected as of late April 2026, escalation in flight. Module 3 ships in some form regardless (PROTECTED, with degraded fallbacks per Module 3 description above).
4. **Korean Google Play Console registration** — partially answered: NEW personal account confirmed (2026-04-08 PM), 14-day closed-test window applies. Identity verification still pending Google's response.
5. **Cafe 홍보 rules** — does 'Let's go 생활체육배구' allow self-promo posts? Deferred to W12-13 acquisition prep block (CEO Decision 32). Affects distribution, not the build.
6. **Default Module 3 schedule** — what cadence should the auto-post default to? Captain can override, but a sensible default reduces onboarding friction. Open until W10 implementation.
7. **Daum cookie freshness detection** — scraper runs each cycle; on HTTP 401/403 or login-page redirect, write a row to a `scraper_failures` table → FCM alert to user (the developer) "Daum cookies expired, refresh required." Manual rotation via `supabase secrets set DAUM_COOKIE="<paste>"`. Acceptable for MVP scale.
8. ✅ **fastexcel-reader on real KakaoBank export** — covered by W1 Gate 1B.
9. **FCM reliability on Korean Galaxy/One UI in 2026** — covered by W1 Gate 1C.
10. ✅ **Google Play Console: NEW personal account** — confirmed 2026-04-08 PM. 14-day closed-testing window applies. Tester recruitment list locked (12+ Android-using humans identified).

## Reviewer Concerns (declined recommendations — accepted risks, plus CEO review additions)

The user explicitly declined recommendations from the design discussion AND from the CEO review. Logging them here so they're visible to future reviewers and to the user themselves before build start.

**CEO review additions (2026-04-08 evening):**

5. **Captain interview format kept at 2 own-club only** (CEO Premise 2). Outside voice argued the single-club sample is a CV-story killer ("I asked my own captain" is a weak interview answer). User explicitly chose build-time over validation breadth. Mitigation: TODOS.md "interview captains from OTHER clubs" entry now has a SHARPENED trigger — if ANY non-own-club closed-test tester churns within 7 days OR opens the app <2 times, halt v1.1 work and do the outside-club interviews immediately.

6. **Path A freemium straddle accepted** (CEO Outside Voice Tension E). Outside voice argued "CV piece with optionality" costs ~1 week of plumbing for a product that may never monetize. User accepted the cost (~30 min subscription scaffold + 2-3 day acquisition prep) as worth it for the optionality. Mitigation: month-3 strategic decision (TODOS.md "Pure CV piece vs real freemium SaaS") is the formal pivot point, not month-6.

7. **Cold-start jankiness from no-Room** (CEO Outside Voice Finding 8). Without Room, every cold-start of every list view requires a Supabase round-trip with Korean cold-start latency. The FCM data-payload trick covers the *one* push-tap path; everything else gets a spinner. User accepted this tradeoff (eng review Decision 2 stands). Mitigation: in production, monitor session duration and uninstall rate. If captains uninstall after a janky cold-start, escalate Room to v1.1 priority.

8. **Closed-test sample bias** (CEO Outside Voice Finding 7). The 12+ closed testers are friends + family + own club, NOT representative strangers. The acquisition prep block (W12-13) tests whether your friends install — not whether strangers do. Real validation happens post-launch via cafe distribution. Acceptable for a pre-product-market-fit project but worth being honest about in CV interviews.

**Original DESIGN.md reviewer concerns (still active):**

1. **Personal Daum account used for scraping.** Recommended a separate scraper-only account. User declined to keep the personal account but accepted slower polling (2-4 hours) as mitigation. **Cost if wrong: irreplaceable personal Daum identity loss.** Mitigation in design: human-like polling cadence, request randomization, real User-Agent headers, manual cookie refresh on failure.

2. **2-4 hour polling cadence (instead of hourly).** Recommended hourly polling for "killer feature" freshness. User chose 2-4 hours for stealth. **Cost if wrong: popular tournaments fill before captains see them, eroding the killer-feature value prop and weakening the CV story.** Mitigation: monitor `time_from_post_to_notification` metric per dispatch and tune cadence after first month of real use.

3. **Captain interview without shadowing.** Recommended shadowing the captain through one full club cycle (practice + tournament app + money cycle) before the interview. User chose interview-only. **Cost if wrong: 2-3 weeks of building modules the captain doesn't actually need.** Mitigation: run the interview in walk-me-through-last-week format with verbatim quotes, no pitching the design.

4. ~~**Module 4 in MVP (declined cut).**~~ **Resolved by CEO Decision 23 (2026-04-08 evening):** what was Module 4 (now Module 3, Band auto-post) is **PROTECTED in MVP**, no longer the cut valve. The original v1/v2 reviewer recommended cutting it; the CEO review reversed that recommendation because the reframed thesis ("operations layer for amateur club captains") is hollow at MVP without all 3 modules. The new cut valve is polish + Maestro coverage. Original concern preserved here for historical traceability only.

## Success Criteria

- App shipped to Korean Google Play Store
- At least 1 verified captain using it daily for ≥2 weeks (the user's own club's captain)
- ≥1 successful payment cycle reconciled end-to-end via Excel import in real club use
- ≥1 tournament discovered and applied via push notification
- Module 3 auto-post running for ≥4 consecutive weeks without manual fallback (or, if Naver Band API access is blocked, the manual clipboard-paste fallback from Module 3's description above)
- Stretch: 3-5 captains in cafe network using it
- Korean-language README + screenshots + 60-second demo video on the Play Store listing
- Open source repo (or private with portfolio access) with clean commit history (portfolio hygiene, not a build task)

## Revenue Model — Path A (Free MVP → freemium at month 6)

The MVP ships free for everyone. Focus is adoption and learning, not monetization. The freemium pivot happens **at month 6, after ≥200 active captains use Libero daily**, not on a fixed calendar date.

> **⚠️ v3 update (CEO review 2026-04-08):** the original Pro tier centered on **auto-Band posting (Module 4)** as the load-bearing "saves time every week" hook. CEO Decision 23 promoted Module 3 (Band auto-post) to PROTECTED in MVP — auto-Band is now a **free-tier feature**. The Pro tier below has been re-cut accordingly. Be honest with yourself: without auto-Band, the Pro tier no longer has a single killer feature — it's a bundle of mid-value perks. Whether that bundle is worth 4,900원/mo to a Korean amateur captain is exactly what the **month-3 strategic decision** in TODOS.md is meant to surface. If month-3 usage signal is weak, this section gets deleted along with the subscription scaffold.

### Tier structure (locked design, deferred activation)

**Free tier (default for everyone — includes the original v2 "killer" feature, auto-Band):**
- 1 club
- Tournament Notifier with 3-hour polling
- Payment Tracker (Excel upload, fuzzy match, alias dict, reference-matched classification)
- **Naver Band auto-post (Module 3)** — recurring weekly practice poll. Was Pro-only in v2; now free per CEO Decision 23.
- Up to 30 members per club
- Up to 12 tournament regions in preferences

**Pro tier (4,900원/mo or 49,000원/yr) — bundle of perks, no single killer feature:**
- **Multi-club management** (captain runs more than one club — rare but high-value)
- **Advanced tournament filters** (skill-level slider, fee range, distance from home, prior-tournament dedup)
- **Priority polling** (1-hour scrape cadence instead of 3 — uses a separate scraper instance to avoid Daum stealth concerns; only Pro clubs get the higher cadence)
- **Excel export** of payment history for tax/club records
- **Custom 미납 reminder templates** (Free uses one canned template)
- **Unlimited members + regions**

**Honest assessment of the Pro hook strength post-CEO-review:** of the 6 Pro features above, only "priority polling" arguably saves time every week, and only for clubs in regions where popular tournaments fill in <3 hours. The rest are nice-to-haves. If the month-3 decision triggers the freemium pivot, expect to either (a) raise priority polling's prominence in the Pro pitch, (b) layer a B2B 클럽 플랜 (Path B) on top, or (c) scope a new Pro-only feature post-launch (e.g., RSVP analytics layered on the v1.1 attendance extension).

### Pricing rationale

- **4,900원/mo** is the standard "low-friction Korean SaaS" price point (Notion 한국, Toss Prime, etc. cluster around 4,900-9,900원/mo). High enough to feel like a real product, low enough that captains don't think twice.
- **49,000원/yr (10 months effective)** is a 16% discount that captures committed power users.
- **Korean payments only at MVP-Pro:** TossPayments + KakaoPay. Stripe later if/when international users appear.

### Realistic revenue projections

| Stage | Active captains | Pro conversion | MRR |
|---|---|---|---|
| Month 6 (freemium launch) | 200 | 5% (10 captains) | ~50,000원 |
| Month 9 | 500 | 7% (35 captains) | ~170,000원 |
| Month 12 | 1,000 | 8% (80 captains) | ~390,000원 |
| Month 18 (best case) | 2,500 | 10% (250 captains) | ~1,225,000원 |

This is **NOT replace-your-job income.** It IS:
- Real paying users on your CV (rare among job applicants)
- Self-sustaining infrastructure (covers ~$5-15/mo cost easily from month 9 onward)
- A real B2B SaaS case study you can talk about in interviews
- Optionality — if it grows beyond projections, you have a real business

### MVP plumbing (week 2 task, before Module 1)

- Add `subscription_tiers` and `club_subscriptions` tables to Supabase (see Data Model)
- Seed `subscription_tiers` with `free` (active) and `pro` (defined but no checkout flow yet)
- Set every new club's `tier_id = 'free'` on creation
- Add a single feature-flag helper in the Android app: `subscription.has(feature: SubscriptionFeature)` that always returns true at MVP, but reads from `club_subscriptions` once Pro launches
- **Do NOT build the checkout flow at MVP.** That's month-6 work. The plumbing is just so the gating logic exists when you flip the switch.

**Cost: ~30 minutes in week 2. Saves 2-3 days of refactoring at month 6.**

### Path B/C migration strategy (deferred decisions)

- **Path B (B2B club subscription):** if the freemium conversion rate is below 5% but the captain interview surfaces "the club would pay" signal, layer a `Club Plan` SKU at 9,900원/mo on top of the existing tiers. The data model already supports this (`tier_id` is a string, just add a new row to `subscription_tiers`).
- **Donations / 토스 송금:** add to settings as a passive option. Doesn't compete with Pro and might earn a few thousand 원 from happy free users.

---

## Distribution Plan

- **Channel 1:** Korean Google Play Store (primary). Free app. Verify whether 사업자등록 is needed for a free Play Store app (lighter requirement than for paid apps, but still confirm).
- **Channel 2:** Post in 'Let's go 생활체육배구' Daum cafe and 1-2 other 9인제 cafes after captain validates. **Pre-requisite: verify cafe 홍보 rules.** If self-promo banned, fallback is 카페매니저 DM or in-thread soft mention.
- **Channel 3:** Tournament-day in-person demos. When user's team plays a tournament, captain shows other captains during downtime. **Personal-install bonus on Android:** for the very first user (your own captain), you can install via `adb install` from your laptop or send a signed APK directly. No need to wait for Play Store rollout. Do NOT host the APK on a public site — that's a different distribution model with its own update/security implications.
- **Phase 2 (post-MVP):** US Google Play listing, English README, bilingual screenshots.
- **Phase 3 (v2):** iOS App Store port via SwiftUI (separate ~6-8 week effort, after Android MVP has real users).
- **CI/CD:** GitHub Actions → Google Play Internal Testing track (instant rollout to up to 100 testers) → Closed Testing → Production. Internal Testing is the equivalent of TestFlight and is the right track for the captain + secretary during the first month.

## Next Steps (concrete build order)

> **⚠️ v3 update:** BUILD.md Part 0 is the canonical day-1 setup (KIPRIS, Play Console, captain interviews, Firebase, Supabase, Anthropic, Naver Band, local tooling). The list below is the v1/v2 prose rewritten to match v3 reality. **No Cloudflare account, no domain purchase** — both eliminated by Decision Reversals 1, 4, 5.

**Pre-build (homework, no code):**
1. **Captain interview** — 30 min × 2 (own captain + own secretary), walk-me-through-last-week format, quotes verbatim. **Confirm captain's phone is Android** (it almost certainly is, but verify on day 1)
2. **KIPRIS trademark check** — search "리베로" and "Libero" in Nice Class 9 (software) and Class 42 (SaaS) at kipris.or.kr. If clean, lock the name. If conflict, fall back to "리베로 캡틴" / "Libero Captain."
3. **Verify cafe 홍보 rules** — read cafe rules, DM 카페매니저 if needed (deferred to W12-13 acquisition prep per CEO Decision 32)
4. **Get a real KakaoBank Excel export** from secretary, document column structure, file type (`.xlsx` vs `.xls`), encoding
5. **Verify Naver Band Open API write scope** — moved to W1 P0 per CEO Decision 24. Process documented in BUILD.md Part 0.7.
6. **Verify Korean Google Play Console registration requirements** for free apps (free apps generally need less than paid apps; confirm the 사업자등록 requirement specifically)
7. **Set up:** Google Play Console ($25 one-time, NEW personal account — start the account today, the 14-day testing clock matters), Firebase project (Auth + FCM only, no Firestore), Supabase project, Anthropic API key, Android Studio + JDK 17. **No Cloudflare account. No domain purchase.** Privacy policy hosting uses Notion public page (W11) per Decision Reversal 6.

**Week 1 (gate — 5 gates per Eng Review Decision 13):**
8. Gate 1A: Daum scraper feasibility (Supabase Edge Function fetch, NOT Cloudflare Workers)
9. Gate 1B: fastexcel-reader smoke test on real KakaoBank `.xlsx`
10. Gate 1C: FCM delivery on real Samsung with battery optimization
11. Gate 1D: Credential Manager + Sign in with Google on Korean OEM
12. Gate 1E: Supabase Edge Function latency from Korea

**Weeks 2-10 (build):**
13. Supabase schema + RLS + Sign in with Google + 6-char invite codes + **subscription scaffold tables (free tier seeded, no checkout flow)** + PIPA captain-attestation pattern (W2 schema column + W4 UI checkbox)
14. Android scaffold (Compose + **Koin** + **in-memory StateFlow**) + onboarding + FCM token registration + Sentry (W3) + GitHub Actions CI (W3) + **`subscription.has(feature)` helper (returns true at MVP)**
15. Module 1 (Tournament Notifier + FCM + Haiku parsing + dispatch + W6 eval suite ≥85% pass + cafe post link button)
16. Internal Testing track with captain + secretary (own club) — gather 1 week of real notification feedback
17. Module 2 (Excel + reference-matched classifier + fuzzy name-match + alias dict + per-row human confirmation)
18. Module 3 (Band auto-post + scheduling + fallback — PROTECTED in MVP, ships in some form even if API approval is blocked)
19. W12-13 acquisition prep block (cafe post wording, in-app KakaoTalk Sharesheet referral, in-person 60-sec demo script) — runs in parallel with the 14-day closed-test calendar window
20. Korean Google Play submission (Internal → Closed 14-day → Production)

**Post-launch:**
21. Distribute via cafe post (after rules verified)
22. v1.1 fast follow: Lineup Planner (1-2 weeks), Kakao Login (~0.5 wk), attendance tracking as M3 extension RSVP-only (~1 wk)
23. **Month 3: strategic CV-vs-SaaS decision.** ≥50 active captains → start 사업자등록 + Toss merchant flow. ≤10 → delete subscription scaffold, commit to pure CV piece. In between → another month of data.
24. **Month 6: freemium activation** (only if month-3 decision triggered the SaaS pivot). Build TossPayments + KakaoPay checkout, flip the gating logic, soft-launch Pro tier to existing users with a 1-month free trial. Only after ≥200 daily active captains.
25. v2 (later): iOS port via SwiftUI rewrite (~6-8 weeks, treated as a separate project)

## What I noticed about how you think

- **You pivoted from Vision OCR to KakaoBank Excel export in one sentence.** That's product judgment most engineers don't have. Your exact words: *"KakaoBank has a 거래내역 다운로드 feature, that sends a excel file to the user's email. The secretary could download this and upload to my app, and since it's a excel file, ai or hardcoded program could analyze it."* You found a structured data source the design was ignoring and routed around the entire fragile-OCR problem. This is the move that turns a weak module into a strong one.

- **You said yes to multi-manager without bargaining.** Your words: *"It's literally just multiple users in one club."* No hedging, no "maybe v2" — you knew from being in the club that the secretary was a real user, and you absorbed the +1 week of work without flinching. You're not optimizing for the smallest possible build, you're optimizing for the build that matches reality.

- **You pushed back on me twice — both times toward accepting risk rather than mitigating it.** You kept the personal Daum account, you skipped shadowing. Both decisions are coherent if your real constraint is "I want to spend my time on the app, not on volleyball anthropology." Just be honest with yourself: if Module 1 ships and notifications are routinely 4 hours late, the killer feature is less killer, and that's the metric the CV story rests on. The interview better be excellent.

- **You said "general portfolio piece" for the CV target.** That tells me you're earlier in the job-hunt arc than someone who'd say "Toss" or "Coupang" — and that's fine. It means the project's value is the *practice* of shipping a real thing, not the optimization for any one employer. Keep the design Korean-first and the screenshots clean and the rest takes care of itself.

- **You pivoted from iOS to Android the same day you locked the design — and you did it for the right reason.** Your words: *"For ages between 30~40, Korean mostly use android phones compared to iphones."* You were sitting with the design and noticed it didn't match the actual user. Most engineers would have built the iOS version they wanted to build and called it good. You tore up the platform decision because it was wrong for the person on the receiving end. That's the move. Match the user, not the developer aesthetic. Same instinct that got you from Vision OCR to KakaoBank Excel — you keep ending up where the real users are. Keep doing that.
